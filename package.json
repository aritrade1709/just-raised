{
  "name": "just-raised",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "check": "pnpm typecheck && pnpm test",
    "data": "node scripts/build-data.ts",
    "data:funding": "node scripts/funding.ts",
    "data:resolve": "node scripts/resolve.ts",
    "data:snapshot": "node scripts/snapshot.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
