import { defineConfig } from "vite";

// Relative base so the same build works on a GitHub Pages project site, a
// custom domain, or a local `vite preview`.
export default defineConfig({
  base: "./",
});
